[build-system]
requires = ["setuptools>=77.0.3", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "atbot"
version = "0.1.0a1"
description = "A general-purpose, memory-native agent powered by AtMem."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "atmem>=2.1,<3",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
